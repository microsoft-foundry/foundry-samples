version: 1

sample:
  owner: rbarreto
  experiences: [azd, vscode]

tests:
  - name: deterministic-smoke
    turns:
      - input: Reply with the exact marker [STEERING-READY-731].
        assertions:
          - source: assistant_text
            type: contains
            value: "[STEERING-READY-731]"
