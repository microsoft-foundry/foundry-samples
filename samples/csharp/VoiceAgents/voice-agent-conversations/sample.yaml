name: Voice Agent Conversations
description: Inspect a stored voice agent conversation - its items and model responses - through the Conversation REST API.

build: "dotnet restore --source https://api.nuget.org/v3/index.json && dotnet build --no-restore --verbosity minimal"
