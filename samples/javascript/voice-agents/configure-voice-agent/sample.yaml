name: Configure a Voice Agent
description: Create a self-deployed voice agent definition with a greeting and disabled conversation storage, then retrieve and delete it.

build: "npm install --registry=https://registry.npmjs.org/"
